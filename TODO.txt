Builtin functions:
	fmod

	trunc
	ceil
	floor

	derivative functions
	Interlocked functions

Language features:
	#include
	#error
	#if
	#else
	#elif
	column_major/row_major matrices
	for statement
	1-dimentional vector types
	struct literals
	arrays
	typedefs
	texture arrays
	arrays of textures

	compute shaders
	structured buffers

Internals:
	make sure all allocations are freed at the end
	some dead code elimination
	cache duplicate constants
	add ability to use same binding for image and sampler to get a combined image sampler

TODAY:

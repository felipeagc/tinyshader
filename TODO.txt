Builtin functions:
	fmod

	trunc
	ceil
	floor

    asuint
    asint
    asfloat
    asdouble

    AllMemoryBarrier
    AllMemoryBarrierWithGroupSync
    DeviceMemoryBarrier
    DeviceMemoryBarrierWithGroupSync
    GroupMemoryBarrier
    GroupMemoryBarrierWithGroupSync

    InterlockedAdd
    InterlockedAnd
    InterlockedCompareExchange
    InterlockedCompareStore
    InterlockedExchange
    InterlockedMax
    InterlockedMin
    InterlockedOr
    InterlockedXor

Preprocessor:
	#error
	#if
	#elif
    function-like #define

Vulkan-specifics:
    push constants
    combined image sampler
    specialization constants

Language features:
    scalar * vector arithmetic

    shared variables
    groupshared variables
    static variables

	struct literals
	arrays
	typedefs
	texture arrays
	arrays of textures

	compute shaders
	structured buffers

    vector casting: float2(uint2(1, 2))

Low priority:
	1-dimentional vector types
	column_major/row_major matrices
    cbuffer
    tbuffer

Internals:
	make sure all allocations are freed at the end
	some dead code elimination
	cache duplicate constants

TODAY:
